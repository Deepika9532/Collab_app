{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env"],

  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },

    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".next/**"]
    },

    "start": {
      "dependsOn": ["build"],
      "cache": false
    },

    "lint": {
      "outputs": []
    },

    "test": {
      "outputs": []
    }
  }
}
